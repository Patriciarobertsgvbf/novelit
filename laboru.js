let searchResults = [];

if (searchResults.length === 0) {
    console.log('The search terms you entered did not return any matches.');
    document.getElementById('search-message').innerText = 'The search terms you entered did not return any matches.';
}
